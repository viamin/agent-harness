{
  "name": "agent-harness-pin-pi",
  "version": "0.0.0",
  "private": true,
  "description": "Version oracle for the Pi coding-agent CLI. Never installed; Dependabot bumps this manifest to signal upstream releases and CI parity spec keeps Providers::Pi::SUPPORTED_CLI_VERSION in sync. See vendor/pins/README.md and issue #336.",
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "0.73.0"
  }
}
